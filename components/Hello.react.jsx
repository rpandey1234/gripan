/** @jsx React.DOM */
React = require('react');
ReactDOM = require('react-dom');

module.exports = Hello = React.createClass({
    // Render the component
    render: function(){
        return (
            <p>Hello gripan</p>
        )

    }
});